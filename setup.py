from setuptools import setup

setup(
    name='xopen3d',
    version='0.1.0',
    packages=['xopen3d'],
    install_requires=[
        'open3d',    
    ],
    description='A package to extend the functionalities of Open3D',
    author='Xi Chen',
)